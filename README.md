# pd_adei_api_container
This branch is dedicated to the Python development (pd) of the API container for ADEI (the company name).  It specifically focuses on implementing, updating, or maintaining the Python code that powers the containerized application's Application Programming Interface (API).
